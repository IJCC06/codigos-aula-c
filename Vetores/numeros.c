#include <stdio.h>

int main()
{
    // Vetor com números declarados no inicio
    // O vetor trabalha com as posições
    // Começa a contagem das posições do 0 (zero)
    int numeros[] = {4, 9, 12, 3};

    printf("Número na Posição 2: %i", numeros[2]);

    return 0;
}